<article class="post">
    <div class="title">
        <a href="/posts/{{id}}">{{title}}</a>
    </div>
    <div class="meta">
        by {{user.username}} on {{format_date created_at}}</div>
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
</article>